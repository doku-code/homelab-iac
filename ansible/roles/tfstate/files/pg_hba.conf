# No production databases or runtime roles are provisioned by this milestone.
local all postgres peer
local all all reject
host all +tfstate_qualification 127.0.0.1/32 scram-sha-256
host all all 0.0.0.0/0 reject
host all all ::/0 reject
