- name: Restart PostgreSQL tfstate cluster
  ansible.builtin.systemd_service:
    name: postgresql@17-main
    state: restarted

- name: Reload tfstate systemd units
  ansible.builtin.systemd_service:
    daemon_reload: true
