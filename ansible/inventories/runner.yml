# Retired CT300 runner inventory: intentionally has no hosts.
# CT300 / 192.168.0.30 now belongs to PostgreSQL (pve-core-tfstate).
# Active runner CT301 remains in runner-migration.yml; do not target tfstate.
all:
  hosts: {}
